actor: FunctionalTester
suite_namespace: Daylog\Tests\Functional
modules:
  enabled:
    - Asserts
    - PhpBrowser:
        url: http://daylog.localhost.test
        headers:
          Accept: 'application/json'
    - REST:
        depends: PhpBrowser
        part: Json